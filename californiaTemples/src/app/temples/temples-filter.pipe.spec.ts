import { TemplesFilterPipe } from './temples-filter.pipe';

describe('TemplesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TemplesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
